import { View } from "react-native";

function Line() {
    return (
        <View
            style={{
                borderBottomWidth: 0.5,
                borderBottomColor: 'rgb(150, 150, 150)',
                marginTop: 30,
                marginBottom: 30,
            }}/>
    );
}

export default Line;